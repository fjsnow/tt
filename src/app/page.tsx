import { Lists } from "./client";

const Page = () => {
    return (
        <div className="flex max-w-2xl w-full mx-auto md:my-16 gap-8">
            <Lists />
        </div>
    );
};

export default Page;
