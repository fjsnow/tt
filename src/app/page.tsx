import { Lists } from "./client";

const Page = () => {
    return (
        <div className="mx-auto flex w-full max-w-2xl gap-8 md:my-16">
            <Lists />
        </div>
    );
};

export default Page;
